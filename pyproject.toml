[project]
name = "ai-council"
version = "0.1.0"
description = "Multi-agent deliberation with Claude"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "openai>=1.50.0",
    "python-dotenv>=1.0.0",
    "uvicorn>=0.32.0",
]

[tool.uv]
package = false
